version: '3.9'
services:
  postgres:
    image: postgres:16-alpine
    container_name: trackd2d_postgres
    environment:
      POSTGRES_USER: trackd2d
      POSTGRES_PASSWORD: trackd2d123
      POSTGRES_DB: trackd2d
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U trackd2d"]
      interval: 5s
      timeout: 5s
      retries: 5
  redis:
    image: redis:7-alpine
    container_name: trackd2d_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
volumes:
  postgres_data:
  redis_data:
